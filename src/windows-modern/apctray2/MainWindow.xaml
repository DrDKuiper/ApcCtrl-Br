<Window x:Class="apctray2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:apctray2"
        Title="apcctrl - Status do UPS" Height="640" Width="800"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        FontFamily="{StaticResource AppFont}">

  <Border Background="{StaticResource BgBaseBrush}" 
          CornerRadius="12" 
          BorderBrush="{StaticResource BorderSubtleBrush}" 
          BorderThickness="1">
    <Border.Effect>
      <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="3" Opacity="0.5" BlurRadius="16"/>
    </Border.Effect>
    
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="40"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Title Bar -->
      <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
          <Ellipse Width="8" Height="8" Fill="{StaticResource AccentBrush}" Margin="0,0,8,0"/>
          <TextBlock Text="apcctrl" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
          <TextBlock Text="  Status do UPS" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,0">
          <Button Content="&#x2500;" Style="{StaticResource TitleBarButton}" Click="Minimize_Click" FontFamily="Segoe UI" FontSize="14"/>
          <Button Content="&#x2715;" Style="{StaticResource CloseButton}" Click="Close_Click" FontFamily="Segoe UI" FontSize="12" Margin="4,0,0,0"/>
        </StackPanel>
      </Grid>

      <!-- Main Content -->
      <Grid Grid.Row="1" Margin="16,4,16,16">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
          <StackPanel Orientation="Horizontal">
            <Border CornerRadius="10" Width="44" Height="44" Margin="0,0,12,0">
              <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                  <GradientStop Color="#3B82F6" Offset="0"/>
                  <GradientStop Color="#8B5CF6" Offset="1"/>
                </LinearGradientBrush>
              </Border.Background>
              <TextBlock Text="&#x26A1;" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
              <TextBlock Text="{Binding UpsName}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
              <TextBlock Text="{Binding Status}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
            </StackPanel>
          </StackPanel>

          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <ComboBox x:Name="UpsSelector"
                      Width="210" Height="30"
                      Margin="0,0,8,0"
                      ItemsSource="{Binding AvailableProfiles}"
                      SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                      DisplayMemberPath="Name"/>
            <ToggleButton x:Name="ThemeToggle" 
                          Width="40" Height="30"
                          Content="&#x263E;"                          IsChecked="{Binding IsDarkMode, Mode=TwoWay}"
                          Background="{StaticResource BgInputBrush}"
                          BorderBrush="{StaticResource BorderSubtleBrush}"
                          BorderThickness="1"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Cursor="Hand"
                          FontSize="16"/>
          </StackPanel>
        </Grid>

        <!-- Highlight Banner -->
        <Border Grid.Row="1" CornerRadius="10" Margin="0,0,0,12">
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#3B82F6" Offset="0"/>
              <GradientStop Color="#8B5CF6" Offset="1"/>
            </LinearGradientBrush>
          </Border.Background>
          <Grid Margin="20,14">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
              <TextBlock Text="RUNTIME" FontSize="10" FontWeight="SemiBold" Foreground="#FFFFFFAA" Margin="0,0,0,4"/>
              <TextBlock Text="{Binding TimeLeft}" FontSize="26" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
            <Border Grid.Column="1" Width="1" Background="#FFFFFF33" Margin="20,0"/>
            <StackPanel Grid.Column="2">
              <TextBlock Text="TEMPERATURA" FontSize="10" FontWeight="SemiBold" Foreground="#FFFFFFAA" Margin="0,0,0,4"/>
              <TextBlock Text="{Binding Temperature}" FontSize="26" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
          </Grid>
        </Border>

        <!-- Metrics Grid -->
        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <!-- Energy Flow -->
          <local:EnergyFlowControl x:Name="EnergyFlow" Grid.Row="0" Grid.ColumnSpan="2" Margin="6"/>

          <!-- Battery -->
          <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}">
            <StackPanel>
              <TextBlock Text="BATERIA" Style="{StaticResource CardTitleStyle}"/>
              <Grid Margin="0,4,0,8">
                <ProgressBar Minimum="0" Maximum="100" 
                             Value="{Binding BatteryPercent}"
                             Foreground="{StaticResource GreenBrush}"/>
                <TextBlock Text="{Binding BatteryPercent, StringFormat='{}{0}%'}" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontWeight="Bold" FontSize="12" Foreground="White"/>
              </Grid>
              <StackPanel Margin="0,4,0,0">
                <TextBlock Text="Saúde da bateria" Style="{StaticResource MetricLabelStyle}"/>
                <TextBlock Text="{Binding BatteryHealthText}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="{Binding BatteryHealthDetails}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
              </StackPanel>
            </StackPanel>
          </Border>

          <!-- Load -->
          <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel>
              <TextBlock Text="CARGA" Style="{StaticResource CardTitleStyle}"/>
              <Grid Margin="0,4,0,8">
                <ProgressBar Minimum="0" Maximum="100" 
                             Value="{Binding LoadPercent}"
                             Foreground="{StaticResource AmberBrush}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                  <TextBlock Text="{Binding LoadPercent, StringFormat='{}{0}%'}" 
                             FontWeight="Bold" FontSize="12" Foreground="White" Margin="0,0,6,0"/>
                  <TextBlock Text="{Binding LoadWatts}" FontWeight="Bold" FontSize="12" Foreground="#FFFFFF99"/>
                </StackPanel>
              </Grid>
            </StackPanel>
          </Border>

          <!-- Input Voltage -->
          <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource CardStyle}">
            <StackPanel>
              <TextBlock Text="TENSÃO ENTRADA" Style="{StaticResource MetricLabelStyle}"/>
              <TextBlock Text="{Binding InputVoltage}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
              <TextBlock Text="{Binding LineFreq}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
          </Border>

          <!-- Output Voltage -->
          <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel>
              <TextBlock Text="TENSÃO SAÍDA" Style="{StaticResource MetricLabelStyle}"/>
              <TextBlock Text="{Binding OutputVoltage}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
          </Border>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
          <Button Content="&#x21BB; Atualizar" Click="Refresh_Click" Style="{StaticResource ModernButton}" Margin="0,0,8,0"/>
          <Button Content="&#x2699; Autoteste" Click="SelfTest_Click" Style="{StaticResource AccentButton}" Margin="0,0,8,0"/>
          <Button Content="Fechar" Click="Close_Click" Style="{StaticResource ModernButton}"/>
        </StackPanel>
      </Grid>
    </Grid>
  </Border>
</Window>
