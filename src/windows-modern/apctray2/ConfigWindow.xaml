<Window x:Class="apctray2.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurações" Height="200" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Grid Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="Host:" VerticalAlignment="Center"/>
      <TextBox x:Name="HostBox" Grid.Column="1" Margin="8,0,0,0"/>
    </Grid>

    <Grid Grid.Row="1" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="Porta:" VerticalAlignment="Center"/>
      <TextBox x:Name="PortBox" Grid.Column="1" Margin="8,0,0,0"/>
    </Grid>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Testar conexão" Margin="0,0,8,0" Click="Test_Click"/>
      <Button Content="Cancelar" IsCancel="True" Margin="0,0,8,0"/>
      <Button Content="Salvar" IsDefault="True" Click="Save_Click"/>
    </StackPanel>
  </Grid>
</Window>
